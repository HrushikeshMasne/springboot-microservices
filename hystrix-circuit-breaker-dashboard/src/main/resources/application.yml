server:
  port: 9195

spring:
  application:
    name: hystrix-circuit-breaker-dashboard

#Adding below configurations in boostrap.yml to fetch config from server.    
##eureka client configurations
#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka   
#  instance:
#    hostname: localhost  
  

#Hystrix dashboard configurations
hystrix:
  dashboard:
    proxy-stream-allow-list: "*"  

